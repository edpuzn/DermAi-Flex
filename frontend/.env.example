# Backend API Base URL
# Use http://127.0.0.1:8000/api for local development outside Docker
# Use http://backend:8000/api if the frontend were server-side rendered (not the case here)
VITE_API_BASE_URL=http://127.0.0.1:8000/api
